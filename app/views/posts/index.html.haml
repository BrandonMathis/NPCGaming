#posts
  - for post in @posts
    .post
      %h3.title
        %a{:href=>"#{edit_post_path(post)}"}= post.title
      .author
        = "by: #{post.user.username}"
      .body
        = post.body
      .footer



%p= link_to "New Post", new_post_path
